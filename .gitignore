# Build artifacts
/target/
debug/
*.pdb

# Cargo lock is committed for binaries but can be ignored for libraries.
# Since this workspace contains binaries, we keep Cargo.lock.

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.*
*.key
*.pem

# Logs
*.log

# Distribution artifacts (built by `make build-pi`)
dist/miseban-agent/miseban-agent
dist/*.tar.gz

# ML models (download via scripts/download-model.sh)
*.onnx
*.pt
models/*.onnx
models/*.pt
